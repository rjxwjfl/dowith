import 'package:flutter/material.dart';

class ObjectTwo extends StatelessWidget {
  const ObjectTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
