import 'package:flutter/material.dart';

class CategoryThree extends StatelessWidget {
  const CategoryThree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
